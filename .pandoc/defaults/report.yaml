# use with `pandoc -dreport <in>.md > <out>.pdf`

from: markdown-implicit_figures
to: pdf

standalone: true
pdf-engine: xelatex

variables:
  geometry:
  - a4paper
  - margin=2cm
  mainfont: "Nimbus Sans"
